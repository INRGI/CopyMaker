import styled from "@emotion/styled";

export const Text = styled.span`
    cursor: pointer;
`;

export const Container = styled.div`
    padding: 0;
    margin: 0;
`;

export const Title = styled.h2`
 padding: 10px 0;
 margin: 0;
`;

export const TitleOfText = styled.strong`
    color: #e40101;
`;